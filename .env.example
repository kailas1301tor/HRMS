# Copy to .env.local for local development (never commit .env.local).
NEXT_PUBLIC_API_URL=https://roka-stage-backend.hroptim.com
# Optional — defaults to "Roka" when unset
NEXT_PUBLIC_PRODUCT_NAME=Roka
